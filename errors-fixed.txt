# Errors fixed & work completed — Saturday, Jul 11, 2026

## Create theme / theme editor bugs

1. **Header add floods extra sections**
   - Fix: adding Header no longer incorrectly inserts a bunch of unrelated elements.

2. **Catalog theme apply brings the same header/section flood**
   - Fix: applying a catalog theme no longer injects unwanted layout sections with Header.

3. **Announcement bar editing panel blank**
   - Cause: TipTap / field grouping / empty panel routing.
   - Fix: announcement section + block settings open again (General / Appearance / Padding / etc.).

4. **Featured product runtime shows Sold out even when stock exists**
   - Fix: sold-out state now respects real product quantities.

5. **Hero marquee editing options not opening**
   - Fix: hero marquee settings panel opens correctly again.

6. **FAQ answers all expanded by default**
   - Cause: editor mode forced answers open.
   - Fix: FAQ answers only show when the accordion item is actually open.

7. **Divider Color scheme removed → Divider settings panel blank**
   - Causes: layout path filter mismatch (`sections.*` vs `.sections.*`), catalog early-return skipping prepare, over-filtering after removing `colorScheme`.
   - Fix: dedicated Divider resolve path with hardcoded fallback fields; settings show again (Background color, Color, Width, Thickness, Length, Padding, Custom CSS) without Color scheme.

8. **Blog posts not showing in blog post elements / carousel**
   - Fix: blogs load and render in blog grid / editorial / carousel sections; blog links configured correctly.

9. **Blog / blog-post templates missing / empty sidebar vs preview**
   - Fix: blog + blog-post templates work like products/collections; default main elements; schema/sidebar wiring so editing matches preview.

10. **Policies and links runtime incomplete**
    - Fix: Policies and links shows store policy links; click opens a modal with that policy content.


## Admin / app UI bugs & restores

11. **`/products/new` page completely blank**
    - Cause: TipTap duplicate extensions (e.g. HorizontalRule / Link) crashing the editor.
    - Fix: duplicate TipTap extensions disabled; add-product page loads.

12. **Add product page still old UI after products restore**
    - Fix: New product form uses the same minimal appearance as edit product / collections.

13. **Pre-merge / pre-commit UI restores**
    - Restored from pre-merge commit state (not memory-only):
      - Blog post / metaobjects-related blog work
      - Tag management
      - Themes option removed from sidebar (pre-commit behavior)
      - Orders section
      - Products section
      - Customers section (Companies removed under Customers)
      - Discounts section
      - Online store section
      - Vendors section


## Storefront / editor features

14. **Apply theme from View button**
    - Fix/feature: under View, added **Apply theme** so the theme can be applied to the store from the editor.

15. **Storefront catalog URL conventions (broken links fixed)**
    - Canonical paths:
      - All products → `/collections/all`
      - Product detail → `/product/{url-handle}`
      - Collections index → `/collections`
      - Collection details → `/collection/{url-handle}`
    - Updated link pickers, menus, presets, theme packs, runtime CTAs, cart links, collection tiles/links, backend menu resolver, theme1/theme2 sources.
    - Shared helpers: `Ziplofy/src/utils/storefront-paths.ts` (+ render-store equivalent).
    - Cursor rule: `.cursor/rules/storefront-urls.mdc` for future work.
    - Admin dashboard `/products` routes left unchanged on purpose.

16. **Featured collection carousel — View all button settings blank / not opening**
    - Cause: catalog nested `view_all_button` only had Label/Link without groups; prepare filtered everything out (incomplete stubs won over full built-in defs).
    - Fix: resolve View all fields with complete Content / Appearance / Size defs; update catalog nested panel; panel opens with Label, open-in-new-tab, Style, colors, and width controls.

17. **Featured collection carousel — Add to cart + product card navigation broken**
    - Cause: shared `FeaturedCollection` runtime had no cart handler and no product `Link`/`productPath` (carousel/grid/editorial all shared this).
    - Fix: quick-add fetches first variant and calls `createCartEntry`; cards link to `/product/{urlHandle}`; preview selection layer allows `[data-codiic-allow-interaction]` so links/cart work in the editor.

18. **`/online-store/themes` layout cluttered / hard to navigate**
    - Fix: simplified to a minimal header (search + view toggle + Create) and one tabbed panel — **Public themes** (catalog), **Installed**, **Custom themes** (user-created) — instead of stacked section cards, stats, tip banners, Creator saves, and Templates.

19. **Admin header search was decorative / no-op**
    - Fix: navbar search now returns rich results — pages & quick actions (products, add product, collections, inventory, etc.), plus live product and collection matches. Click / Enter navigates; Ctrl+K focuses search.

20. **Admin header chrome cleanup**
    - Removed Show Tour button, question-mark setup guide, and bell/notifications from the navbar.

21. **`/online-store/themes` Custom themes tab empty**
    - Cause: tab listed legacy HTML `/custom-themes` instead of theme-creator saves (`store-custom-themes`).
    - Fix: Custom themes now loads and shows themes saved from Create theme (Open / Install / Delete).

22. **Admin search results missing nav hierarchy**
    - Searching e.g. “inventory” only showed the leaf title.
    - Fix: catalog items include `navPath`; results show breadcrumb trails like `Products › Inventory` (and nested Settings/Content/etc.). Live product/collection hits show their path too.

23. **Custom theme cards — Open → Edit**
    - Custom themes tab primary action label changed from “Open” to “Edit”.

24. **`/online-store` page — placeholder / non-functional**
    - Rebuilt as a minimal overview: live storefront iframe preview (real store URL), current live theme with Customize/Change, and Manage links (Themes / Pages / Preference). Removed fake images, theme gallery fluff, and dead CTAs.

25. **Announcement bar — remove Theme Settings & Custom CSS**
    - Editing panel no longer shows Theme Settings (color scheme) or Custom CSS; keeps General / Appearance / Padding only.

26. **Announcement block Typography Font options not applying**
    - Font role mapping ignored Subheading/Accent in horizon preview; Size was overridden by mobile `font-size: 13px !important`.
    - Fix: resolve Body/Subheading/Heading/Accent via shared font helpers (with role weight/style), stop forcing 13px on mobile, ensure Font select always has options.

27. **Announcement text textarea lag while typing**
    - Debounced announcement block text field (350ms local draft + flush on blur) so typing no longer thrashes preview updates.

28. **Header menu — Create menu from Select menu**
   - Menu picker dropdown now includes a **Create menu** action; opens a bottom sheet to name the menu, add items, save, and auto-select it for the header.

29. **Header logo — Store name not editable**
   - Logo settings only showed visibility/padding; store name was hardcoded as “My Store”.
   - Fix: Logo panel now includes Store name (+ Tagline) fields wired to `sections.header.blocks.logo.settings.text`.

30. **Create menu sheet — free-text links**
   - Theme-editor Create menu bottom sheet used a plain text link input.
   - Fix: reuse the same prefixed link picker as `/content/menus` (Home, Collections, Products, etc.).

31. **Header menu — phantom “specific product/collection” links after apply**
   - Applying a store menu updated config, but stale `items.N.label/href` sidebar values (schema still declares 4 nested link slots) were merged back via `applyValuesToThemeConfig`, re-expanding the nav with leftover links. A `startTransition` race made this worse.
   - Fix: prune stale item value paths to the applied menu length, sync values immediately, resolve nav labels from collection/product titles (never raw linkType strings), and clear stale linkType when editing links.

32. **Header Cart bubble Custom colors not applying in editor**
   - Bubble only rendered when live cart count &gt; 0, so Custom background/text colors never appeared in the empty editor preview.
   - Fix: show a sample cart count in editor preview; seed default Custom colors when switching to Custom; write hex fields as `color` type.

33. **Header editing — remove Theme Settings & Custom CSS**
   - Header settings panel no longer shows Theme Settings or Custom CSS; keeps Logo / Menu / Colors / Cart bubble / Page backgrounds / etc.

34. **Header Menu background color not applying**
   - Menu Appearance background was resolved from config but never painted on the nav.
   - Fix: apply menu background (and padding) to desktop + mobile menu surfaces; treat `default`/empty as transparent inherit.

35. **Rich text — image fit + overlay controls**
   - Background image had no Cover / Fit / Stretch control; overlay toggle used a hardcoded dark wash with no color or opacity.
   - Fix: add Image fit (cover / fit / stretch) when media is Image; when Background overlay is on, show Overlay color + Overlay opacity and apply them in the Rich text runtime.

36. **Rich text Heading/Text shows raw HTML in preview**
   - Richtext editor stores values like `<p>f</p>`, but preview rendered them as plain text children.
   - Fix: render heading and text via `ThemeEditorRichTextContent` so HTML is parsed in the live preview.

37. **Rich text — cannot remove Heading / Text / Button**
   - Those items are settings-backed virtual blocks; delete tried to remove non-existent `blocks.*` entries, so they always came back.
   - Fix: track active items in `settings.contentBlocks`, remove/add updates that list, sidebar + preview respect it, and Add block can restore Heading / Text / Button.

38. **Rich text Background color not applying**
   - Home-page (template) schema omitted `backgroundColor`, so picker values never merged into preview config.
   - Fix: add the field to the template schema, fall back to layout field types when merging values, and ensure the Appearance panel always exposes Background color on the correct section path.

39. **Rich text — remove Custom CSS**
   - Custom CSS option removed from the Rich text settings panel (schema sidebar + panel groups).

40. **Rich text Background color still not updating in preview**
   - Cause: picker values could be skipped when schema type lookup missed remapped/instance paths; horizon preview `RichTextSection` also ignored `backgroundColor` and always used the color scheme.
   - Fix: fall back to text/color types when merging unknown `*Color` paths; apply `backgroundColor` in both create-theme and horizon preview runtimes; keep Appearance field matching on `.backgroundColor`; surface Color scheme in Appearance again.

41. **Rich text Heading — Custom typography preset**
   - Heading Preset only had Default / H1–H4 with no freeform controls.
   - Fix: add **Custom** preset under Rich text → Heading → Typography with Font, Size, Line height, Letter spacing, Case, and Wrap; apply those settings in the Rich text runtime.

42. **Featured collection Product card → Media settings ignored**
   - Sidebar Media options (aspect ratio, borders, padding) saved to `product_card.settings.media*`, but Featured Collection runtime used global theme product-media + a hardcoded 1:1 CSS aspect ratio.
   - Fix: read and apply card media aspect ratio, border style/radius, and padding on carousel/grid/editorial product cards.

43. **Featured collection product card links used Mongo ids / old paths**
   - Storefront collection (and public store) product list APIs omitted `urlHandle`, so cards linked to `/product/{_id}` instead of `/product/{urlHandle}`.
   - Fix: include `urlHandle` (and SEO fields) in those list projections so carousel cards use handle URLs.

44. **Featured collection grid — remove Theme Settings & Custom CSS**
   - Grid editing sequence no longer shows Theme Settings (color scheme) or Custom CSS; keeps Collection / Section layout / Padding.

45. **Featured collection grid — Theme Settings still visible**
   - Stale "Carousel" sidebar label / shared `navIcon` field defs made the panel miss the grid variant and fall through to the generic groups (which still show Theme Settings).
   - Fix: resolve variant from layoutType/catalogVariant first; always strip Theme Settings & Custom CSS for grid; keep featured-collection sections out of the generic grouped panel.

46. **Featured collection grid — Theme Settings accordion still showing**
   - Color scheme was still in the shared field defs and grid sometimes remapped away from the hide path.
   - Fix: remove Theme Settings / Custom CSS from the base Featured collection field defs; only inject them for carousel/editorial; grid panel group order ends at Padding.

47. **Featured product — remove Theme Settings & Custom CSS**
   - Featured product section settings no longer show Theme Settings (color scheme) or Custom CSS; keeps Product / Layout / Padding.

48. **Featured product — selected product / variant data not shown in runtime**
   - Picker cache fields (`productTitle` / `price` / `productImageUrl`) could be dropped when schema types missed remapped paths; editor also skipped variant fetch/clicks.
   - Fix: always merge those product picker fields into config; always fetch product + variants by id; allow variant selection in preview; show price/image from the selected variant.

49. **Featured product — fake color swatches showing**
   - Variant picker Swatches defaulted on and rendered hardcoded placeholder circles (`#111827` / `#6b7280` / `#d1d5db`), not real product colors.
   - Fix: remove the placeholder swatch UI; default Swatches to off.

50. **Featured product — remove recipient email & message fields**
   - Buy buttons showed a gift card form (recipient email + optional message) when `giftCardForm` was enabled.
   - Fix: remove the gift card form from runtime; drop Gift card products settings from the Buy buttons panel.

51. **Contact form section settings mislabeled as FAQ**
   - Contact form shares Layout/Size fields with FAQ, so FAQ detection ran first and relabeled the panel “FAQ”.
   - Fix: exclude `contact_form`/`email_signup` paths from FAQ field detection; resolve Contact form / Email signup before FAQ in sidebar prepare; detect Contact form panel before FAQ in the settings panel.


## Notes

- Legacy storefront paths (`/products/...`, `/collections/{handle}`) still redirect where supported.
- Hard-refresh / Vite restart may be needed after schema/catalog JSON changes.
