/** Pack default is used on clone; optional tweaks after insert. */
export function applyAnnouncementBarPreset(_section: Record<string, unknown>): void {
  /* Uses blueprint from theme.default-config.json */
}
