import React from 'react';
import { SectionPreviewCard } from '../_shared/SectionPreviewCard';

export function featuredcollectiongridPreview() {
  return <SectionPreviewCard label="Featured collection: Grid" variant="featured-collection-grid" />;
}
