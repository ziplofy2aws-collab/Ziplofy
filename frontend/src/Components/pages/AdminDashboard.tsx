import React from "react";

// Intentionally empty to avoid background panel rendering on every page.
// Keep the file for route compatibility if needed in the future.
const AdminDashboard: React.FC = () => {
  return null;
};

export default AdminDashboard;


