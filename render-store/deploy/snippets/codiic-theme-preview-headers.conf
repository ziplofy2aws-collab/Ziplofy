# Include on preview.codiic.com (all locations) and on merchant store /theme-preview.
# CSP frame-ancestors overrides X-Frame-Options in modern browsers, so the theme
# editor on dashboard.codiic.com / admin.codiic.com can embed this document.
add_header Content-Security-Policy "frame-ancestors *" always;
