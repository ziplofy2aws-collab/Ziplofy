# Proxy storefront SEO files to Ziplofy3b before SPA fallback.
# Place these locations ABOVE `location / { try_files ... /index.html; }` in each store vhost.

location = /sitemap.xml {
  proxy_pass http://127.0.0.1:5000/sitemap.xml;
  proxy_set_header Host $host;
  proxy_set_header X-Forwarded-Host $host;
  proxy_set_header X-Forwarded-Proto $scheme;
}

# Optional direct API path (useful when nginx only proxies /api to backend):
# location = /api/storefront/sitemap.xml {
#   proxy_pass http://127.0.0.1:5000/api/storefront/sitemap.xml;
#   proxy_set_header Host $host;
#   proxy_set_header X-Forwarded-Host $host;
#   proxy_set_header X-Forwarded-Proto $scheme;
# }
