Today's Work — Thursday, June 19, 2026
======================================

1. Storefront SEO — Products
   - Root cause: storefront links use URL handles (/products/my-handle) but SEO loaders only fetched products by MongoDB id, so page title and meta description never applied on live product pages.
   - Backend: added GET /products/public/store/:storeId/url-handle/:urlHandle; refactored getProductByIdPublic to share buildPublicProductDetailResponse (includes pageTitle, metaDescription, urlHandle).
   - render-store: added fetchProductForRoute(storeId, routeParam) in product.context.tsx — resolves either ObjectId or URL handle.
   - Updated StorefrontProductSeoLoader and StorefrontProductPreviewLoader to use store id + route param for SEO data.
   - Ziplofy3b backend typecheck passed (tsc --noEmit).

2. Storefront SEO — Collections
   - Storefront collection-by-handle API now explicitly returns pageTitle and metaDescription.
   - StorefrontCollectionByUrlHandleLoader: skip collection metadata lookup for /collections/all (avoids 404 clearing state); post list failures no longer block blog-style metadata loading pattern mirrored for collections where relevant.
   - Admin save for collection SEO (page title, meta description, url handle) was already working; fix was on storefront load + StorefrontSeoManager.

3. Storefront SEO — Blogs & blog posts (articles)
   - storefront-blog.controller: explicit selects for blog and post SEO fields (pageTitle, metaDescription, urlHandle, content, etc.).
   - Added render-store/utils/storefront-path-handle.util.ts for consistent URL handle normalization across loaders and API calls.
   - StorefrontBlogByUrlHandleLoader: separate try/catch so blog SEO metadata loads even if post list fetch fails.
   - StorefrontBlogPostByUrlHandleLoader: clears stale blog/post state on navigation; uses shared path handle util.
   - ThemePageRouteElements: blog and blog-post loaders now run alongside CustomThemeTemplatePage (same pattern as collections/products), not only the built-in StorefrontBlogPage.
   - resolve-storefront-seo: canonical URLs use saved blog/post handles; added unit tests for blog and blog post SEO.
   - Vitest: resolve-storefront-seo.test.ts — 5 tests passing.

4. SEO audit — what’s covered vs not
   - Done: products, collections, blogs, blog posts/articles.
   - Already wired (verify in browser): homepage SEO via Online store preferences (seoHomePageTitle, seoMetaDescription on store).
   - Not needed yet: gift card products (admin has SEO fields but no dedicated storefront route; linked products use product SEO), Online store Pages (placeholder UI only), shop metafields (placeholder).
   - Generic routes (cart, login, profile, etc.) use fixed titles, not admin SEO.
   - Liquid/SSR storefront path may still use basic titles instead of pageTitle — only affects Liquid-only themes, not React/custom-theme storefront.
