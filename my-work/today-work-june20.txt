Today’s work log — June 20, 2026
================================
(Keep updating this file as more work is done today.)

ORDERS — TABLE & UI (Shopify-style)
-----------------------------------
- Orders page: header (Export, More actions, Create order), filters, table columns
- Customer column: popover on name click (portal + fixed positioning) — name, location, order count, email, View customer
- Items column: popover with fulfillment badge, delivery method, line items (portal fix — no clipping in table)
- Export orders modal: scopes (current page, all, selected, search, by date), CSV formats (Excel / plain)
- Removed “Export transaction histories” from export modal (orders + transaction CSV helper cleaned up)
- More actions → “Show analytics bar” / “Hide analytics bar” toggle
- Analytics bar (dummy UI): Today / Last 7 days / Last 30 days, metric cards, horizontal scroll + arrow nav
- Create order (Orders page header + empty state) → `/orders/drafts/new`

DRAFT ORDERS
------------
- Drafts list page: minimal empty state (Create draft order → `/orders/drafts/new`)
- New page `/orders/drafts/new`: two-column layout (Products, Payment | Notes, Customer, Markets, Tags)
- Route ordering fixed: `/orders/drafts/new` and `/orders/drafts` before `/orders/:id`
- SEO title for `/orders/drafts/new`

ABANDONED CARTS
---------------
- List page: minimal header (inline stats line), table layout (no loud stat cards)
- Customer column: name + chevron popover (email, phone, cart info, View customer) — same portal pattern as Orders
- Removed “View” from Actions; row click still opens cart details
- Details page: minimal layout — back link, title + subtitle, Send recovery email, line items table + sidebar (Customer, Cart summary)
- Send recovery email modal: minimal shell (template, subject, message) + ProductDescriptionInput rich text editor (images/templates disabled)
- Recovery email templates simplified to 2 only: Classic recovery + Friendly reminder (removed discount codes, free shipping, urgency/scarcity templates)
- Recovery emails: professional store-branded HTML layout (header, CTA button, footer); sends via API to developer200419@gmail.com (test mode)

FILES TOUCHED (main areas)
--------------------------
- Ziplofy/src/pages/OrdersPage.tsx
- Ziplofy/src/pages/orders/DraftsPage.tsx, CreateDraftOrderPage.tsx, AbandonedCartPage.tsx, AbondonedCartDetailsPage.tsx
- Ziplofy/src/components/orders/* (table, export, analytics, draft-order, abandoned cart components)
- Ziplofy/src/App.tsx, resolve-admin-seo.ts
- Ziplofy/src/utils/recovery-email-templates.ts, send-abandoned-cart-recovery-email.ts
- Ziplofy3b/src/controllers/cart-recovery.controller.ts, cart.route.ts

NOT DONE / FOLLOW-UPS
---------------------
- Analytics bar: real data (currently dummy)
- Draft order: save/API, product picker, payment actions
- Export “Orders by date”: optional validation if both dates empty
- Abandoned cart: actual recovery email send via POST `/storefront/cart/store/:storeId/recovery-email` (test recipient hardcoded)

PRODUCTS
--------
- Products list header: removed Export, Import, and More actions — Add product only
- After creating a product: redirect to `/products/:id` with “Added {title}” banner (Add another product only)
- Products filters bar: removed edit-columns / filter icon button

COLLECTIONS LIST
----------------
- `/products/collections`: Shopify-style layout (header, All/Published/Draft filter + search, table with Title/Products/Product conditions)
- API: store collections list now includes `productCount` per collection
- Collection create + details: minimal Shopify-style form (shared header, sections, gray-900 save, quiet cards)
- After creating a collection: redirect to `/products/collections/:id` with “Added {title}” banner (Add another collection only)
