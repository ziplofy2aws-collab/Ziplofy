import React from 'react';
import { SectionPreviewCard } from '../_shared/SectionPreviewCard';

export function collectionlinksspotlightPreview() {
  return <SectionPreviewCard label="Collection links: Spotlight" variant="collection-links-spotlight" />;
}
