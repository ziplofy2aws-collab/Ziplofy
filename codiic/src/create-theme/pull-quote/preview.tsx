import React from 'react';
import { SectionPreviewCard } from '../_shared/SectionPreviewCard';

export function pullquotePreview() {
  return <SectionPreviewCard label="Pull quote" variant="pull-quote" />;
}
