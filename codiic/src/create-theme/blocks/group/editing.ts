import type { CreateThemeBlockEditing } from '../types';

export const editing: CreateThemeBlockEditing = {
  "blockId": "group",
  "label": "group",
  "settingsOrder": [
    {
      "key": "text",
      "label": "Content",
      "type": "text"
    }
  ]
};
