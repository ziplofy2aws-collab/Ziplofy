# Production build for auth.codiic.com
# Copy to .env.production before `npm run build`, or set in CI / EC2.

VITE_API_URL=https://backend.codiic.com/api
VITE_REDIRECTION_URL=https://dashboard.codiic.com
# VITE_GOOGLE_CLIENT_ID=<from Google Cloud Console — authorized origin https://auth.codiic.com>
